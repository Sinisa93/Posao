namespace Argosy.Common.Enums
{

    /// <summary>
    /// Use to customize the object mapping results for each of the generic front end searches.
    /// </summary>
    public enum RecordSizeCategory
    {
        Tiny = 0,
        Small = 20,
        Medium = 40,
        Normal = 60,
        Large = 80,
        All = 100
    }
}
